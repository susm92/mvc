{% extends "base.html.twig" %}

{% block title %}API Block{% endblock %}

{% block body %}
<h1>API Home</h1>

<p>Below are all the JSON routes this website offers, you can read a bit of what all redirects before clicking on them</a>.</p>

<form method="post" action="">
    <ul class="api-deck">
        <li>
            <input type="submit" name="quote" value="Quote"
                onClick="this.form.action='{{ path('api_quote') }}';this.form.method='get';"
            > -- Directs the user to a website, which shows a json response with a random quote, together with the date and time of the current day.
        </li>
        <li>
            <input type="submit" name="deck" value="Deck"
                onClick="this.form.action='{{ path('api_deck') }}';this.form.method='get';"
            > -- Shows the entire deck of cards, this shows the deck sorted by color in rising size.
        </li>
        <li>
            <input type="submit" name="shuffle" value="Shuffle"
                onClick="this.form.action='{{ path('api_shuffle_post') }}';"
            > -- Shuffles the entire deck and later returns an JSON structure of the deck, does also restore the deck.
        </li>
        <li>
            <input type="submit" name="draw" value="Draw"
                onClick="this.form.action='{{ path('api_deck_draw_post') }}';"
            > -- Draws one card from the deck, each time a card is decreased from the current deck.
        </li>
        <li>
            <input type="submit" name="number" value="Number"
                onClick="this.form.action='{{ path('api_number_cards_post', {num: 5}) }}';"
            > -- Draws an amount of cards from deck, per default it's set to 5, you can edit the URL to draw even more cards.
        </li>
        <li>
            <input type="submit" name="game" value="Game Status"
                onClick="this.form.action='{{ path('get_api_game') }}';this.form.method='get';"
            > -- Redirects you to a webiste where the current status of the game is displayed, game must be started for this to work.
        </li>
        <li>
            <input type="submit" name="library" value="Show Library"
                onClick="this.form.action='{{ path('get_api_library') }}';this.form.method='get';"
            > -- Shows all the current books in the library, all is displayed in JSON-data.
        </li>
        <li>
            <input type="submit" value="ISBN Search"
                onClick="event.preventDefault(); window.location.href='{{ path('get_api_isbn_book', {isbn: 9789173539678}) }}';"
            > -- Redirects the user to a specific ISBN search of a book, the URL can be changed to another specifc ISBN number... See this as an example.
        </li>
    </ul>
</form>

{% endblock %}